package steps

import (
	"fmt"
	"unidriver/errors"
)

func init() {
	StepList["assertElementStyle"] = assertElementStyle
}

func assertElementStyle(_url interface{}) {

	url := _url.(string)

	fmt.Print("[assertElementStyle]: " + url + " ")
	err := WD.Get(url)
	errors.Fatal(err)
	fmt.Print("SUCCESS")
	fmt.Println("")

}
