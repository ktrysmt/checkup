package steps

import (
	"fmt"
	"checkup/errors"
)

func init() {
	StepList["acceptAlert"] = acceptAlert
}

func acceptAlert(_url interface{}) {

	url := _url.(string)

	fmt.Print("[acceptAlert]: " + url + " ")
	err := WD.Get(url)
	errors.Fatal(err)
	fmt.Print("SUCCESS")
	fmt.Println("")

}
