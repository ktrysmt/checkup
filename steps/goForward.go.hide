package steps

import (
	"fmt"
	"checkup/errors"
)

func init() {
	StepList["goForward"] = goForward
}

func goForward(_url interface{}) {

	url := _url.(string)

	fmt.Print("[goForward]: " + url + " ")
	err := WD.Get(url)
	errors.Fatal(err)
	fmt.Print("SUCCESS")
	fmt.Println("")

}
