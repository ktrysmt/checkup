package steps

import (
	"fmt"
	"checkup/errors"
)

func init() {
	StepList["mouseOverElement"] = mouseOverElement
}

func mouseOverElement(_url interface{}) {

	url := _url.(string)

	fmt.Print("[mouseOverElement]: " + url + " ")
	err := WD.Get(url)
	errors.Fatal(err)
	fmt.Print("SUCCESS")
	fmt.Println("")

}
